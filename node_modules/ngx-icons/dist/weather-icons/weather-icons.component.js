import { Component, Input } from '@angular/core';
var WeatherIconsComponent = (function () {
    function WeatherIconsComponent() {
        this._options = [];
    }
    WeatherIconsComponent.prototype.ngOnInit = function () {
        if (!this.name) {
            throw new Error('Missing name for Weather Icons Component');
        }
        if (this.name === 'wind' && !this.direction) {
            this.direction = 'towards';
        }
        if (this.name === 'wind' && !this.cardinal && !this.degree) {
            this.degree = 0;
        }
        if (this.name === 'time' && (!this.time || this.time < 1 || this.time > 12)) {
            this.time = 1;
        }
        if (this.fixed) {
            this.addOption("wi-fw");
        }
        if (this.rotate === 90 || this.rotate === 180 || this.rotate === 270) {
            this.addOption("wi-rotate-" + this.rotate);
        }
        if (this.flip === 'horizontal' || this.flip === 'vertical') {
            this.addOption("wi-flip-" + this.flip);
        }
        if (this.name === 'wind' && (this.direction === 'towards' || this.direction === 'from')) {
            if (this.degree) {
                this.addOption(this.direction + "-" + this.degree + "-deg");
            }
            else if (this.cardinal) {
                this.addOption("wi-" + this.direction + "-" + this.cardinal);
            }
        }
        else if (this.name === 'wind-beaufort') {
            if (!this.beaufort || (this.beaufort > 11 || this.beaufort < 0)) {
                this.beaufort = 0;
            }
            this.name = this.name + "-" + this.beaufort;
        }
        else if (this.name === 'time') {
            this.name = this.name + "-" + this.time;
        }
    };
    Object.defineProperty(WeatherIconsComponent.prototype, "options", {
        get: function () {
            return this._options;
        },
        enumerable: true,
        configurable: true
    });
    WeatherIconsComponent.prototype.addOption = function (addClass) {
        this._options.push(addClass);
    };
    return WeatherIconsComponent;
}());
export { WeatherIconsComponent };
WeatherIconsComponent.decorators = [
    { type: Component, args: [{
                selector: 'wi',
                template: "\n    <i class=\"wi wi-{{name}}\" [ngClass]=\"options\" aria-hidden=\"true\"></i>\n  "
            },] },
];
/** @nocollapse */
WeatherIconsComponent.ctorParameters = function () { return []; };
WeatherIconsComponent.propDecorators = {
    'name': [{ type: Input },],
    'size': [{ type: Input },],
    'fixed': [{ type: Input },],
    'rotate': [{ type: Input },],
    'flip': [{ type: Input },],
    'direction': [{ type: Input },],
    'degree': [{ type: Input },],
    'cardinal': [{ type: Input },],
    'beaufort': [{ type: Input },],
    'time': [{ type: Input },],
};
//# sourceMappingURL=weather-icons.component.js.map